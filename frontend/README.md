# Novel Agent MVP Frontend

Vue 3 + TypeScript frontend for the novel-writing Agent MVP.

## Install

```powershell
npm install
```

## Run

```powershell
npm run dev
```

The Vite dev server proxies `/api` to `http://localhost:8000`.

## Build

```powershell
npm run build
```
